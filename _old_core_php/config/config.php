<?php
// Server path (for PHP includes)
if (!defined("BASE_PATH")) {
    define("BASE_PATH", __DIR__ . "/../");
}

// Browser path (for links, CSS, images, etc.)
if (!defined("BASE_URL")) {
    define("BASE_URL", "http://localhost/eulozia/");
}
?>